# CloudLink Navigator

CloudLink Navigator 是一个面向技术团队、开源社区运营者与个人开发者的外链资源聚合与导航工具。该项目并非传统意义上的内容管理系统，而是一套以静态站点为核心、以结构化资源目录为载体的轻量级技术导航框架。其核心定位在于将分散于网络各处的高价值技术文档、社区论坛、数据看板、监控面板与运营后台入口，整合为一份可版本化、可协作、可自动化更新的外链清单。

项目目标用户包括：需要维护团队公共书签库的运维工程师、需要统一管理多环境后台入口的研发经理、以及希望对外公开呈现技术生态全貌的开源项目维护者。CloudLink Navigator 解决的问题是“链接失序”，即当团队或社区拥有的外部资源数量超过二十个之后，通过浏览器书签或即时通讯工具传播链接的方式将显著降低信息可达性，并增加新人上手成本。本项目提供一套约定优于配置的目录结构，使链接的增删改查能够通过 Markdown 与 YAML 的联合描述完成，并支持生成可供浏览器起始页、若依框架后台或 Vue 管理面板直接消费的 JSON 数据接口。

## 功能概览

- **多级目录分类引擎**：支持基于文件系统的三级目录结构映射，每一级目录对应一个业务领域或技术分层，目录名称即分类标签，无需额外配置数据库。

- **外链元数据标注体系**：每条链接可附带标题、简短描述、维护人、更新周期、可用性状态（有效/待验证/已迁移）以及标签组，便于后续检索与自动化监控。

- **静态数据导出协议**：内置数据导出脚本可将目录与链接信息转换为 JSON、YAML 或 HTML 列表格式，便于嵌入现有管理后台、监控面板或文档站点。

- **链接可用性检查接口**：提供基于 HTTP 状态码的批量链接探测工具，支持通过命令行参数指定并发数与超时阈值，输出不可用链接清单。

- **版本化变更记录**：每个链接的新增、修改与删除操作均通过 Git 提交记录体现，配合 CHANGELOG 生成器可自动生成外链变更周报。

- **权限分级占位机制**：在目录结构中预留 public/internal/private 三级占位目录，便于团队区分对外公开链接、内部使用链接与敏感后台入口，但项目本身不实现鉴权逻辑，仅提供组织规范。

- **多实例环境切换模板**：支持通过环境变量或配置文件前缀（dev/test/prod）生成不同环境下的链接完整路径，适用于 API 网关地址、日志平台入口等多环境资源。

## 应用场景

- 开源社区文档中心导航：社区维护者可将所有周边工具、镜像站、CI/CD 状态页、会议录播地址归类整理，随项目文档一同发布，新贡献者可通过一份导航快速了解社区基础设施全貌。

- 企业内部技术中台入口聚合：中小型技术团队可将开发环境 Jenkins、GitLab、ArgoCD、Grafana、ElasticSearch 管控台等数十个后台地址统一维护，新员工入职时仅需获取一个仓库地址即可访问全部必要系统。

- 个人技术研究书签库迁移：技术研究者可将日常阅读的技术博客、在线编译器、API 测试工具、性能对比网站等外链导出为结构化数据，配合静态站点生成器打造个人化的技术起始页，并跨设备同步。

- 多环境测试入口管理：QA 团队可为同一套测试系统在 dev、staging、production 环境下的不同入口分别标注环境标签，通过一份目录同时管理三套环境的跳转地址，避免混淆。

## 快速开始

以下步骤将引导您在本地完成项目的克隆、依赖安装与初次运行。

```bash
# 1. 克隆项目仓库
git clone https://github.com/cloudlink-navigator/navigator.git
cd navigator

# 2. 安装依赖（项目使用 Python 3 编写核心脚本，依赖见 requirements.txt）
pip install -r requirements.txt

# 3. 运行本地数据预览服务（默认监听端口 8080）
python serve.py --port 8080

# 执行完毕后，访问 http://localhost:8080 即可查看当前目录下的链接导航页面。
# 若需更新链接数据，请直接编辑 data/ 目录下的 Markdown 文件或 YAML 索引。
```

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.8 及以上 | 用于运行数据解析脚本、本地预览服务与链接探测工具 |
| Git | 2.20 及以上 | 用于克隆仓库、提交变更记录与版本回退 |
| yaml | PyYAML 5.4.1 及以上 | 用于解析目录索引文件中的 YAML 元数据 |
| markdown | 3.3.4 及以上 | 用于将目录描述文件中的 Markdown 标题与列表转换为 HTML |
| requests | 2.25.0 及以上 | 用于链接可用性检查模块发送 HTTP 探活请求 |
| click | 8.0.0 及以上 | 用于构建命令行交互接口，提供子命令解析能力 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|---|---|---|
| 入门指南 | docs/getting-started.md | 如何理解目录结构、如何新增第一条外链、如何验证本地预览效果 |
| 目录规范 | docs/directory-spec.md | 每一级目录的命名约束、索引文件的格式要求、元数据字段的定义与可选性 |
| 导出与集成 | docs/export-formats.md | 如何生成 JSON 供前端消费、如何将导航数据嵌入已有 Vue 或 React 项目 |
| 自动化运维 | docs/health-check.md | 如何配置定时任务执行链接探测、如何解读探测报告并批量更新失效链接 |

## 资源列表

以下为项目当前版本收录的全部外部资源链接，按类别分组呈现。所有链接均保留用户原始输入格式，未做任何协议补充或域名改写。

技术数据看板

<code>yingchaobifen.cn</code>

<code>bingdaochaosaicheng.net.cn</code>

<code>xijiajishibifen.com.cn</code>

<code>bingdaochaojifenbang.net.cn</code>

<code>yijiabifen.cn</code>

官方信息门户

<code>bifenguanfang.org.cn</code>

<code>bifenguanwang.com.cn</code>

## 项目结构

```
navigator/
├── data/                               # 核心数据目录，存放所有外链分类文件
│   ├── public/                         # 对外公开链接分类
│   │   ├── community/                  # 社区相关资源（论坛、会议、镜像）
│   │   ├── docs/                       # 技术文档与规范地址
│   │   └── tools/                      # 在线工具与测试平台
│   ├── internal/                       # 内部使用链接（不对外发布）
│   │   ├── ci-cd/                      # CI/CD 流水线与制品库入口
│   │   └── monitoring/                 # 监控告警与日志查询平台
│   └── private/                        # 敏感后台入口占位（仅目录结构，无实际数据）
├── scripts/                            # 命令行工具脚本集
│   ├── check_links.py                  # 批量链接可用性探测脚本
│   ├── export_json.py                  # 导出 JSON 格式数据供前端使用
│   └── generate_changelog.py           # 基于 Git 日志生成外链变更周报
├── templates/                          # 静态页面生成模板
│   ├── index.template.html             # 导航首页 HTML 骨架
│   └── category.template.html          # 分类详情页模板
├── serve.py                            # 本地预览服务启动入口
├── requirements.txt                    # Python 依赖声明文件
└── README.md                           # 项目说明文档（即本文档）
```

## 贡献指南

我们欢迎并鼓励社区贡献者参与外链资源的补充与更新。所有贡献均需遵循以下流程：

1. 复刻项目仓库至个人名下，并在本地切换到专用分支，分支命名建议采用 `feat/add-{category}-links` 或 `fix/update-{domain}` 格式，以便识别变更范围。

2. 在 `data/` 目录下定位到合适的分类子目录，编辑对应的 Markdown 文件或 YAML 索引。新增链接时需提供完整标题、目标 URL（按原始格式填写）、简短描述以及维护人标识。若不确定分类归属，可在 `data/public/community/` 下新增临时文件并标注 `[WIP]` 前缀。

3. 提交变更前需在本地执行链接探测脚本 `python scripts/check_links.py --path data/`，确保新增或修改的链接返回 HTTP 2xx 或 3xx 状态码。若链接为内网地址且无法在公网探测，请在内网验证后在提交说明中标注 `[internal-only]`。

4. 推送分支至远程仓库，并向主仓库发起 Pull Request。PR 描述中需包含变更类别（新增/更新/删除）、受影响分类以及是否涉及环境切换模板的改动。PR 需至少获得一位维护者的代码审核与功能验证后方可合并。

5. 合并后，维护者将触发 CI 流程重新生成静态导航页面与 JSON 数据文件，并在约五分钟内更新至预览站点。贡献者将在下一次周报中自动列入致谢名单。

## 常见问题

**Q: 项目是否支持 HTTPS 链接与裸域名混用？**

A: 完全支持。项目不对链接格式做任何自动补全或改写。用户输入的链接原样保存在元数据中，导出时同样保留原始格式。但链接探测脚本会基于 HTTP 协议发起请求，因此若裸域名未配置默认协议，探测结果可能不准确，建议在链接描述中注明协议偏好。

**Q: 如何将本项目与现有的 Vue 或 React 管理后台集成？**

A: 项目根目录下的 `scripts/export_json.py` 脚本可将全部数据导出为结构化 JSON 文件。您可在后台项目中通过 HTTP 请求获取该 JSON 文件，或将其作为构建产物打包至前端静态资源中。若需实时同步，建议配合 CI 流程在每次合并后自动生成 JSON 并上传至您的 CDN 或 API 服务。

**Q: 链接数量过多时，本地预览服务是否会卡顿？**

A: 本地预览服务采用惰性加载策略，仅在请求特定分类时解析对应目录。当链接总数超过 2000 条时，建议使用 `--cache` 参数启用内存缓存，或直接使用导出脚本生成静态 HTML 而非依赖实时解析。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-07-22 11:10:39
