# NavHub 技术资源导航站

NavHub 是一个面向开发者和技术研究人员的轻量级外链资源聚合与导航系统，专用于整理、分类和快速检索互联网上的高频访问技术站点、赛事数据接口、实时信息面板与运维工具。项目定位为“技术团队的入口层基础设施”，不提供数据存储，不生产内容，仅作为结构化转发层与可定制的起始页，帮助团队将零散的外部依赖 URL 集中管理，降低新成员上手成本，减少书签栏混乱。

目标用户包括：运维工程师、数据运营人员、赛事信息爬虫开发者、前端脚手架维护者以及需要频繁查阅外部实时榜单的技术管理者。NavHub 通过静态配置驱动，支持单页快速渲染，天然适配内网穿透或本地容器化部署，也可直接托管至对象存储作为纯静态站点使用。项目本身不依赖数据库，所有外部链接以声明式清单维护，变更可追溯，回滚可验证，适合作为内部技术文档体系中的导航子模块。

## 功能概览

- 多级分类面板：支持按技术领域、数据源类型、业务线自定义分组，每组独立维护链接清单，分类间互不干扰。
- 实时可用性探测：后台异步对已注册外链发起 HEAD 请求，超时阈值可配置，面板中自动标记异常节点并记录不可用时长。
- 一键复制与直达：每个链接条目提供“复制原始地址”与“新窗口打开”双操作按钮，减少重复输入与误触跳转。
- 关键字模糊检索：基于链接标题、描述、分类标签进行实时本地全文匹配，不依赖后端服务，响应时间控制在 200 毫秒内。
- 链接变更审计日志：所有增删改操作记录至本地 JSON 差分文件，支持按时间轴回溯，便于排查误操作或外部地址迁移。
- 布局密度切换：提供“紧凑”“舒适”“宽松”三种视觉密度模式，适应大屏监控与小屏笔记本不同使用习惯。
- 配置热加载：修改链接配置文件后，页面通过轮询机制自动刷新数据层，无需重启服务或重新构建。

## 应用场景

1. 赛事数据聚合面板：用于实时监控多个外部赛事比分、积分榜与赛程信息。NavHub 将分散的赛果与赛程页面聚合为统一入口，数据运营人员可一键切换不同赛事数据源，避免反复手动输入域名或检索历史记录。

2. 技术文档快速跳转站：作为团队内部 Confluence 或 GitBook 的前置引导页，集中放置常用 CI/CD 工具链、镜像仓库、API 网关控制台及日志分析平台地址，新入职开发者可在数分钟内完成常用环境的书签初始化。

3. 外链健康度巡检工具：配合定时任务（如 Jenkins 或 CronJob）定期打开 NavHub 页面，利用其内置的可用性探测功能，自动生成外部依赖可用性日报，及时通知责任人处理第三方站点超时或证书过期问题。

4. 本地开发环境起始页：开发者在本地启动容器集群或微服务网关后，通过 NavHub 快速访问各服务自带的 Swagger UI、Metrics 端点、分布式追踪面板，无需记忆不同端口的访问路径。

5. 会议大屏共享导航：在技术评审或故障复盘会议中，将 NavHub 投屏至会议电视，所有参会人员可共同浏览同一组外部资源，避免各自打开不同浏览器标签导致信息不一致。

## 快速开始

以下命令适用于 Linux / macOS / Windows WSL2 环境，请确保已安装 Git 与 Node.js 18.0 及以上版本。

```bash
# 克隆项目仓库
git clone https://github.com/navhub/navhub-starter.git navhub

# 进入项目目录
cd navhub

# 安装依赖（使用 npm 或 yarn）
npm install

# 启动本地开发服务器，默认监听 3000 端口
npm run dev
```

启动后，在浏览器中访问 `http://127.0.0.1:3000` 即可看到默认示例面板。若需修改链接清单，请编辑 `config/links.json` 文件，保存后页面自动刷新。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于开发服务器及构建脚本 |
| npm | 8.x 或 9.x | 包管理器，用于安装依赖及执行脚本命令 |
| Git | 2.30 及以上 | 版本控制工具，用于克隆仓库及提交配置变更 |
| 浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 运行时客户端，需支持 ES2020 语法及 Fetch API |
| 对象存储（可选） | 兼容 S3 协议 | 用于生产环境静态文件托管，如 MinIO、AWS S3、阿里云 OSS |
| 反向代理（可选） | Nginx 1.18+ / Caddy 2 | 用于绑定自定义域名及启用 HTTPS，非必需 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 使用者 | /docs/quick-start.md | 如何在 5 分钟内完成首次部署并添加第一个外部链接 |
| 使用者 | /docs/customization.md | 如何修改页面标题、Logo、配色方案及布局密度默认值 |
| 维护者 | /docs/maintenance.md | 如何批量迁移链接、处理失效地址、导出审计日志 |
| 贡献者 | /docs/architecture.md | 项目的模块划分、数据流方向、扩展点设计及接口约定 |
| 贡献者 | /docs/testing.md | 本地如何运行单元测试、E2E 测试以及性能基准测试命令 |

## 资源列表

本导航站默认收录以下外部资源作为示例数据源，用户可根据需要保留、删除或替换。所有资源按类别分组展示。

赛事数据类

- <code>fajiajishibifen.net.cn</code>
- <code>yingchaobisaijieguo.net.cn</code>
- <code>yijiasaicheng.net.cn</code>
- <code>yijiabisaijieguo.net.cn</code>
- <code>ouxielianzigesaibisaijieguo.org.cn</code>
- <code>yingchaojifenbang.net.cn</code>
- <code>ouxielianzigesaisaicheng.org.cn</code>

## 项目结构

```
navhub/
├── config/                        # 全局配置文件目录
│   ├── links.json                 # 核心链接清单（分类、标题、URL、标签）
│   ├── theme.json                 # 主题变量（颜色、字体、间距、密度预设）
│   └── probe.json                 # 可用性探测参数（超时、重试次数、间隔）
├── src/
│   ├── core/                      # 核心逻辑层
│   │   ├── linker.js              # 链接解析、去重、排序与过滤引擎
│   │   ├── probe.js               # HEAD 请求探测与状态缓存管理
│   │   └── diff.js                # 链接变更差分生成与审计日志写入
│   ├── ui/                        # 视图层组件
│   │   ├── PanelRenderer.js       # 分类面板渲染器，支持虚拟列表
│   │   ├── SearchBar.js           # 本地全文检索组件与高亮逻辑
│   │   └── DensitySwitch.js       # 密度模式切换控制器
│   ├── adapters/                  # 外部存储适配器（用于生产环境）
│   │   ├── s3Adapter.js           # 从 S3 桶读取 links.json 并缓存
│   │   └── localAdapter.js        # 从本地文件系统读取配置
│   └── entry/                     # 入口文件
│       ├── index.js               # 主程序入口，初始化路由与挂载根组件
│       └── worker.js              # 后台 Service Worker，用于探测任务调度
├── public/                        # 静态资源目录
│   ├── favicon.ico                # 站点图标
│   └── offline.html               # 离线降级提示页
├── tests/                         # 测试套件
│   ├── unit/                      # 单元测试（Jest）
│   └── e2e/                       # 端到端测试（Playwright）
├── scripts/                       # 构建与运维辅助脚本
│   ├── build-static.js            # 生成全量静态 HTML 用于托管
│   └── migrate-links.js           # 从旧格式 CSV 或 JSON 迁移至新结构
├── .env.example                   # 环境变量模板（端口、S3 密钥等）
├── package.json                   # 项目依赖与脚本定义
├── README.md                      # 项目总览文档（即当前文件）
└── LICENSE                        # MIT 许可证文本
```

## 贡献指南

1. 复刻仓库并创建特性分支：从主仓库复刻至个人账户，随后在本地基于 `main` 分支创建 `feature/your-feature-name` 分支，禁止直接在 main 分支上修改。

2. 遵守代码规范与测试要求：提交前请运行 `npm run lint` 和 `npm run test`，确保无 ESLint 警告且所有单元测试通过。若新增功能，需同步补充对应测试用例。

3. 更新链接清单时的特殊流程：若贡献内容涉及 `config/links.json` 的变更，请在 Pull Request 描述中附上变更原因（如原地址失效、新增权威数据源等），并确认所有 URL 均未包含私有 IP 或内网敏感地址。

4. 签署开发者原创声明：首次提交 Pull Request 时，需在 PR 评论中明确声明“本人保证所提交内容均为原创或已获得合法授权，且不包含任何恶意链接或侵权内容”。

5. 等待至少一名维护者代码审查：维护者将在 3 个工作日内给出审阅意见。如需紧急合入，请在工作日 10:00-17:00 间联系 @navhub-core 团队。

## 常见问题

Q: 我可以通过 NavHub 代理外部资源内容以绕过跨域限制吗？

A: 不可以。NavHub 明确不充当代理服务器或反向代理，也不提供内容缓存或转发能力。项目仅作为链接聚合与状态探测工具，所有外部资源仍需由用户浏览器直接请求。任何试图将本工具用于规避同源策略或网络访问控制的行为均超出许可范围。

Q: 生产环境部署后，页面一直显示“探测超时”，但外部站点实际可访问。如何调整？

A: 请编辑 `config/probe.json` 文件，增大 `timeout` 字段（单位毫秒），例如从默认的 3000 调整为 8000。同时检查 `retries` 字段，若网络环境波动较大，建议设置为 2 或 3。调整后保存文件，页面将在下一次轮询周期（默认 60 秒）自动生效。

Q: 我想在内网完全离线使用 NavHub，但页面加载时仍会请求外部字体或 CDN 资源。如何处理？

A: 项目默认使用了 Google Fonts 和 CDN 托管的图标库。如需完全离线，请修改 `src/ui/theme.js` 中的字体引用为系统字体栈（如 `-apple-system, Segoe UI, Roboto`），同时将图标库替换为本地 SVG 图标或 Unicode 字符。此操作不影响核心导航功能，但会改变视觉呈现风格。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-07-22 11:10:39
