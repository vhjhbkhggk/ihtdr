# OSS-Nav

一个面向开源技术爱好者和基础设施运维人员的结构化外链资源导航系统。该项目定位于对互联网上分散的、高质量的技术文档、社区论坛、赛事数据以及标准化组织公告进行集中化索引与分类展示，解决开发者在信息检索过程中面临的资源碎片化、权威性难以甄别以及检索效率低下的问题。目标用户包括系统架构师、开发者、技术研究人员以及高校相关专业师生。

## 功能概览

- **资源分级索引**：按照技术领域、数据来源、地理区域等多维度对收录的链接进行层级划分，支持快速筛选。
- **数据源状态监控**：定期对收录的URL进行可用性探测，并在界面中标记异常状态，便于维护者及时清理或更新。
- **分类标签系统**：每条外链资源均支持打上多组分类标签，用户可根据标签进行交叉检索，缩小定位范围。
- **自定义收藏集**：允许注册用户将常用链接归入个人收藏夹，并支持添加备注信息，方便团队内部共享上下文。
- **RESTful API 查询接口**：提供标准化的JSON API接口，允许第三方工具或脚本以编程方式查询资源列表及元数据信息。
- **全文检索支持**：集成轻量级全文检索引擎，支持对资源标题、描述、标签以及来源组织名称进行关键词匹配。
- **变更历史记录**：记录每条资源链接的新增、修改和失效状态变更时间戳，便于追溯信息演变过程。

## 应用场景

- **技术选型调研**：当团队需要对某一技术领域（如容器编排、服务网格）进行选型时，可通过该导航站快速获取相关领域的技术白皮书、官方文档链接以及社区活跃讨论区，显著缩短信息收集周期。
- **赛事数据聚合查询**：各类技术竞赛或行业联赛的实时比分和积分榜数据分散在多个不同域名下，运维人员可通过本系统的聚合视图统一关注多个赛事数据源的状态变更。
- **标准化组织文档追踪**：需要跟进欧洲或国内标准化组织最新发布的技术规范或赛程公告时，可利用导航站对相关组织官网的索引，快速跳转至公告栏目，避免逐级翻阅官网层级。
- **离线文档镜像规划**：企业内网环境下的运维工程师可根据导航站列出的资源清单，规划需要离线缓存的关键文档站点列表，用于构建内部镜像站。
- **学术研究数据采集**：高校研究人员在进行互联网数据采集或网络行为分析时，可将导航站作为初始种子URL列表的来源，系统化展开数据爬取任务。

## 快速开始

以下步骤适用于Linux / macOS / Windows WSL环境，需提前安装Git与Node.js 18+。

```bash
# 克隆项目仓库至本地
git clone https://github.com/oss-community/oss-nav.git

# 进入项目根目录
cd oss-nav

# 安装项目依赖（使用npm）
npm install

# 复制环境变量示例文件并填写必要配置
cp .env.example .env

# 初始化本地SQLite数据库并导入默认资源索引
npm run init-db

# 以开发模式启动本地服务，默认监听端口3000
npm run dev
```

启动成功后，访问控制台输出的本地地址（通常为 http://localhost:3000 ）即可使用导航界面。如需构建生产环境静态文件，执行 `npm run build` 后配合 `npm run start` 启动。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行服务端脚本及构建工具链 |
| npm | 对应Node.js内置版本 | 包管理器，用于安装项目声明的所有依赖库 |
| SQLite3 | 3.38.0 或更高 | 嵌入式数据库，用于存储资源索引及用户数据；生产环境可更换为PostgreSQL |
| Git | 2.25.0 或更高 | 版本控制工具，用于克隆仓库及获取提交历史 |
| 操作系统 | Linux内核 4.0+ / macOS 11+ / Windows 10 版本 2004+ | 支持Node.js运行的主流操作系统均可 |
| 网络连通性 | 外网可访问 | 首次启动时需从npm官方源及GitHub拉取依赖；内网部署需配置镜像源 |
| 内存 | 不低于512 MB | 开发模式下内存占用约300 MB，生产环境可降低至200 MB以下 |
| 磁盘空间 | 不低于200 MB | 包含源代码、依赖包及数据库文件的总占用空间 |

## 文档导航

| 层面 | 目录 / 文档路径 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `docs/user-guide/` | 如何使用搜索、收藏及分类过滤功能；如何订阅资源变更通知 |
| 运维手册 | `docs/operations/` | 如何部署至生产服务器；如何配置反向代理；如何执行数据库备份与恢复 |
| API 参考 | `docs/api/` | 如何调用RESTful接口获取资源列表；接口鉴权方式；错误码含义 |
| 贡献者指引 | `CONTRIBUTING.md` | 如何提交新资源链接；如何修改分类体系；代码提交规范及PR流程 |
| 设计文档 | `docs/design/` | 系统架构图、数据模型ER图、缓存策略及扩展性设计说明 |
| 测试指南 | `docs/testing/` | 单元测试与集成测试的执行命令；如何添加新的测试用例覆盖新增资源 |

## 资源列表

本部分按数据来源类别划分，收录当前系统索引的全部外链资源。每个URL均以原始格式呈现，不进行任何协议补全或域名规范化处理。

赛事积分与比分数据类

- <code>beimailiansaibeibisaijieguo.org.cn</code>
- <code>oulianzigesaijifenbang.org.cn</code>
- <code>ouguanzigesaibifen.org.cn</code>
- <code>fajiasaicheng.org.cn</code>
- <code>ouxielianzigesaibifen.org.cn</code>
- <code>xijiabisaijieguo.net.cn</code>
- <code>ouxielianzigesaijishibifen.org.cn</code>

以上链接均作为独立数据源纳入每日健康检查队列。系统将分别对每个域名执行TLS证书有效期验证及HTTP状态码检测，异常结果将记录至管理后台仪表盘。

## 项目结构

项目采用分层架构设计，前端UI与后端API服务分离，共用数据模型层。

```
oss-nav/
├── frontend/                   # 前端单页应用源码
│   ├── src/
│   │   ├── pages/              # 路由页面组件（首页、分类页、收藏页）
│   │   ├── components/         # 可复用UI组件（导航卡片、搜索栏、标签过滤器）
│   │   ├── hooks/              # 自定义React Hooks（数据请求、本地存储）
│   │   └── styles/             # 全局样式表及主题变量
│   ├── public/                 # 静态资源（favicon、站点图标）
│   └── package.json            # 前端构建依赖声明
├── backend/                    # 后端API服务及数据库适配层
│   ├── src/
│   │   ├── controllers/        # 请求控制器（资源查询、用户认证、收藏管理）
│   │   ├── models/             # 数据模型定义（资源、标签、用户、变更日志）
│   │   ├── services/           # 业务逻辑层（健康检查、索引更新、检索算法）
│   │   ├── routes/             # RESTful路由注册及中间件配置
│   │   └── utils/              # 工具函数（日志、配置解析、加密）
│   ├── migrations/             # SQLite/PostgreSQL数据库迁移脚本
│   └── package.json            # 后端服务依赖声明
├── docs/                       # 完整项目文档（见文档导航章节）
├── scripts/                    # 运维辅助脚本（数据导入、备份、健康检查触发）
├── tests/                      # 单元测试与集成测试用例
│   ├── unit/                   # 服务层与工具函数单元测试
│   └── integration/            # API接口端到端测试
├── .env.example                # 环境变量配置模板
├── docker-compose.yml          # 容器化编排配置（含数据库与Redis）
├── Dockerfile                  # 生产环境镜像构建文件
└── README.md                   # 项目入口说明文档
```

## 贡献指南

我们欢迎社区贡献者以多种形式参与本项目建设，包括但不限于新增资源链接、修复数据源链接失效、优化检索算法以及完善文档。

1.  **认领或提出议题**：在GitHub Issues页面查看待处理任务列表，或新建议题描述您希望改进的功能点或新增的数据类别。对于新增外链资源，请附上资源用途说明及官方性质佐证。
2.  **派生仓库并创建特性分支**：将本仓库派生至您的个人账户下，然后基于 `main` 分支创建以 `feature/` 或 `fix/` 为前缀的新分支，例如 `feature/add-football-data-sources`。
3.  **执行变更并遵守代码规范**：所有代码变更需通过ESLint与Prettier格式化检查，新增外链需同步更新 `backend/migrations` 目录下的种子数据文件，并确保 `docs/` 下相应文档得到同步修订。
4.  **编写或更新测试用例**：对于新增API接口或修改核心检索逻辑，必须补充对应的单元测试或集成测试，确保原有测试套件全部通过。
5.  **发起拉取请求**：向本仓库的 `main` 分支发起Pull Request，在描述中清晰引用相关Issue编号，并列出变更摘要。至少需要一位项目维护者进行代码审查，审查通过后即可合并。

## 常见问题

**问：导航站收录资源链接的筛选标准是什么？是否接受个人博客或非官方来源？**

答：当前索引主要面向技术社区、标准化组织、官方赛事数据发布站点以及经过验证的开源项目文档。原则上优先收录域名主体清晰、内容长期维护且与基础设施或开发技术直接相关的站点。个人博客或非官方来源若内容质量较高且具有一定行业影响力，可经由贡献者提交并经过维护团队投票表决后纳入索引。所有新增链接均需在提交时附带简要说明。

**问：系统如何处理外链资源失效或域名过期的情况？**

答：后端服务内置了定时任务，每24小时对所有已收录URL执行一次HEAD请求检测。连续三次检测返回非2xx状态码或连接超时时，该资源将被自动标记为“疑似失效”，并在前端界面以视觉弱化样式展示。同时，变更历史表中会记录每次失效状态切换的时间点。维护人员定期审查标记条目，确认失效后将其迁移至“已归档”分类，不再参与日常检索。

**问：是否支持私有化部署并在内网环境中使用？**

答：完全支持。项目采用SQLite作为默认数据库，无需外部数据库服务即可运行，非常适合内网或离线环境。您只需将源代码及依赖包完整拷贝至内网机器，通过环境变量关闭外网健康检查功能（`HEALTH_CHECK_ENABLED=false`），并手动导入所需资源数据的JSON导出文件即可。Docker镜像也提供了针对内网部署的优化构建参数。

## 许可证

本项目采用 MIT 许可证进行开源分发。您可以自由使用、修改、复制、分发本软件，或将其用于商业目的，无需征得额外书面授权。完整的许可证文本请参见项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-07-22 11:10:39
