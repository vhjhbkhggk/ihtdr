# Bifrost Link Hub

Bifrost Link Hub 是一个面向中文互联网技术内容创作者、站长及数字资源管理者的高可用外链聚合与导航系统。项目定位于解决多源技术文档、社区论坛、个人博客及项目主页在分散部署环境下，入口地址频繁变动、记忆成本高、检索效率低的问题。系统以静态站点形式提供结构化资源目录，支持快速分类检索、状态监控与批量导入导出，适用于中小型技术团队、独立开发者及开源项目维护者作为统一的对外信息窗口。

目标用户包括：需要维护多个子站点或服务入口的运维工程师、拥有多平台技术专栏的博主、开源项目生态中的文档聚合者，以及希望建立内部技术资源知识库的研发小组。Bifrost Link Hub 不提供动态数据存储，专注于链接资源的组织与呈现，可与现有监控系统或静态站点生成器无缝集成。

## 功能概览

- **多级分类目录**：支持无限层级的资源分类，可根据技术栈、平台性质或使用频率自定义分组，便于构建符合业务场景的导航树。

- **批量链接导入导出**：提供标准 CSV 与 JSON 格式的批量操作接口，支持从现有站点地图、书签文件或数据库导出数据快速初始化系统。

- **可用性被动检测**：集成轻量级链接状态检查模块，定期对收录的域名发起 HEAD 请求，在管理面板中标记异常状态并生成变更日志。

- **标签与全文检索**：为每个资源条目附加自定义标签，配合基于倒排索引的标题及描述搜索，实现毫秒级响应，支持模糊匹配与拼音首字母检索。

- **访问统计与点击追踪**：记录每个外链的点击次数与最后访问时间，提供按热度排序的视图，帮助管理员识别高频使用的核心资源。

- **响应式管理面板**：提供面向管理员的独立操作界面，支持链接增删改查、分类迁移、状态批量更新，所有操作均记录操作日志并支持回滚。

- **静态导出与部署**：一键生成完整的静态 HTML 目录站点，可直接部署至对象存储或 CDN，不依赖数据库或后端运行时环境，降低维护成本。

## 应用场景

1. **技术团队内部知识导航**：研发团队可将日常使用的代码仓库地址、持续集成服务面板、日志查询平台、项目文档站点等分散入口统一收录至 Bifrost Link Hub，新成员入职时仅需访问一个入口即可获取全部必要资源，减少沟通成本。

2. **开源项目生态外链聚合**：开源项目维护者可将项目主仓库、示例代码库、社区论坛、问题追踪系统、版本发布页、贡献者文档等关联站点集中展示，方便使用者快速跳转至所需模块，提升项目整体可用性体验。

3. **个人技术博客的多平台联动**：独立技术博主可将其发布在不同写作平台（如语雀、知乎专栏、微信公众号文章存档）、自建博客、GitHub Pages 项目页以及视频教程合集的外链统一管理，形成完整的内容分发矩阵，便于粉丝订阅与回溯。

4. **线下技术沙龙的资源指引**：组织线下 meetup 或线上直播活动时，主办方可创建临时分类，收录讲师资料、演示文稿链接、报名表单、回放视频地址及互动问答面板，活动结束后保留历史档案供后续参考。

5. **域名与站点迁移过渡管理**：当业务系统进行域名更换或架构升级时，可通过 Bifrost Link Hub 同时展示新旧入口地址，并标注迁移状态与生效时间，引导用户逐步切换，降低访问中断风险。

## 快速开始

以下步骤帮助您在本地环境快速启动 Bifrost Link Hub 开发实例。

```bash
# 克隆项目仓库
git clone https://github.com/bifrost-team/bifrost-link-hub.git

# 进入项目目录
cd bifrost-link-hub

# 安装依赖（使用 npm 或 yarn）
npm install

# 复制环境变量配置模板
cp .env.example .env

# 启动开发服务器，默认监听 3000 端口
npm run dev
```

启动成功后，访问控制台输出中显示的本地地址（通常为 http://localhost:3000）即可查看示例导航页面。使用默认管理员账户（需参考 `.env` 中初始化的账号密码）登录管理面板，开始录入您的第一个链接分类与条目。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 18.0.0 | 运行时环境，用于构建与开发服务 |
| npm | >= 9.0.0 或 yarn >= 1.22.0 | 包管理器，用于依赖安装与脚本执行 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及提交更新 |
| 操作系统 | Linux / macOS / Windows (WSL2 推荐) | 支持主流操作系统，Windows 下建议使用 WSL2 以提升文件系统性能 |
| 浏览器 | Chrome >= 90 / Firefox >= 88 / Edge >= 90 | 管理面板需使用现代浏览器以支持 Web Components 与 ES Module |
| 磁盘空间 | >= 200 MB | 安装依赖及构建产物所需的最小空间，实际建议预留 1 GB |
| 内存 | >= 2 GB | 开发模式下内存占用较高，生产构建建议 4 GB 以上 |
| 网络 | 可访问公共 npm registry | 用于拉取项目依赖包，可配置私有镜像源加速 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|------|----------|----------|
| 用户指南 | `/docs/user-guide/getting-started.md` | 如何快速配置第一个分类并添加链接？如何导入现有书签数据？ |
| 管理员手册 | `/docs/admin/management-panel.md` | 管理面板各模块的功能详解，批量操作与状态监控如何使用？ |
| 开发者文档 | `/docs/developer/api-reference.md` | 系统内部数据模型与存储接口说明，如何扩展自定义分类器或检测插件？ |
| 部署运维 | `/docs/deployment/static-export.md` | 如何将系统导出为纯静态站点并部署至 Nginx、CDN 或对象存储？ |
| 架构设计 | `/docs/architecture/overview.md` | 系统前后端分离的架构图、数据流向、核心模块职责划分。 |
| 变更日志 | `/CHANGELOG.md` | 每个版本新增功能、修复缺陷及破坏性变更记录。 |
| 安全策略 | `/SECURITY.md` | 报告安全漏洞的联系方式与响应流程，以及默认账户安全建议。 |

## 资源列表

本系统初始资源库收录了以下中文互联网域名，按类别组织供快速检索与跳转。所有条目均保留用户提供的原始格式，未做任何协议或子域名补充。

官方主站类别

- <code>bifenguanfang.cn</code>

核心域名类别

- <code>bifenguanwang.net.cn</code>
- <code>bifenguanfang.net.cn</code>
- <code>bifenguanwang.cn</code>

组织与备用域名类别

- <code>bifenguanwang.org.cn</code>

赛事与专项类别

- <code>xijiasaicheng.org.cn</code>
- <code>ruidianchaobisaijieguo.org.cn</code>

上述资源条目在系统初始化时已预置为只读示例数据，管理员可编辑描述、分类及标签，但系统默认保留原始 URL 不变。新增自定义条目时，同样建议遵循用户原始输入规则，不自动补全协议或添加尾部斜杠，以确保跳转行为符合预期。

## 项目结构

项目采用模块化分层设计，以下为关键目录与文件说明：

```
bifrost-link-hub/
├── src/                                # 核心源代码目录
│   ├── api/                            # 后端接口层（Node.js + Express）
│   │   ├── routes/                     # RESTful 路由定义（links, categories, stats）
│   │   ├── controllers/                # 业务逻辑控制器（增删改查、状态检测）
│   │   └── middleware/                 # 身份验证、日志记录、错误处理中间件
│   ├── frontend/                       # 前端应用（React + TypeScript）
│   │   ├── pages/                      # 页面级组件（主页、分类视图、管理面板）
│   │   ├── components/                 # 可复用 UI 组件（导航树、搜索框、统计卡片）
│   │   ├── stores/                     # Zustand 状态管理（链接列表、筛选条件、用户会话）
│   │   └── styles/                     # 全局样式与主题变量（CSS Modules + Tailwind）
│   ├── core/                           # 与框架无关的核心逻辑
│   │   ├── link-validator/             # 链接可用性检测器（支持 HEAD 请求与超时重试）
│   │   ├── importer/                   # CSV / JSON 导入解析器（字段映射与校验）
│   │   └── exporter/                   # 静态站点生成器（模板引擎 + 路由聚合）
│   └── config/                         # 应用配置加载器（环境变量、默认分类预设）
├── tests/                              # 单元测试与集成测试（Jest + Supertest）
│   ├── unit/                           # 核心模块单测（验证器、导入器、分类树）
│   └── integration/                    # API 端到端测试与数据库交互测试
├── docs/                               # 项目文档（用户手册、开发者指南、部署方案）
│   ├── user-guide/                     # 面向最终用户的操作说明
│   ├── admin/                          # 面向管理员的配置与调优文档
│   ├── developer/                      # 面向贡献者的架构与接口文档
│   └── deployment/                     # 面向运维人员的部署与监控指南
├── scripts/                            # 辅助脚本（数据迁移、种子数据生成、健康检查）
├── public/                             # 静态资源（favicon、robots.txt、sitemap 模板）
├── .env.example                        # 环境变量配置模板（端口、数据库路径、密钥）
├── package.json                        # 项目依赖与脚本定义（npm / yarn）
├── tsconfig.json                       # TypeScript 编译配置（严格模式、路径别名）
├── vite.config.ts                      # 前端构建工具配置（开发代理、构建优化）
└── README.md                           # 项目概述与快速入门（即本文档）
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于功能建议、缺陷报告、文档改进和代码提交。请遵循以下流程以确保协作顺畅：

1. **阅读行为准则与安全策略**：在参与贡献前，请先查阅 `CODE_OF_CONDUCT.md` 与 `SECURITY.md` 文件，了解社区基本规则及漏洞报告渠道。

2. **提交议题（Issue）**：对于新功能请求或缺陷反馈，请先在 GitHub Issues 中搜索是否已有类似议题。若不存在，请使用提供的议题模板详细描述复现步骤、预期行为与实际结果，并标明运行环境版本。

3. **派生（Fork）与分支开发**：将主仓库派生至个人账户后，基于 `develop` 分支创建以 `feature/` 或 `fix/` 为前缀的新分支进行开发。请确保分支名称简洁描述变更内容。

4. **编码规范与测试**：所有 JavaScript/TypeScript 代码必须通过 ESLint 与 Prettier 格式化校验。新增或修改功能需编写对应的单元测试或集成测试，确保测试覆盖率达到 80% 以上。

5. **发起拉取请求（Pull Request）**：完成开发与本地测试后，向主仓库的 `develop` 分支发起 PR。PR 描述中需关联对应的议题编号，并简要说明变更动机、实现方案及影响范围。维护者将在 3 个工作日内进行审阅。

## 常见问题

**问：系统是否支持多用户协同管理链接资源？**

当前版本聚焦于单管理员模式，所有操作通过共享的本地存储或 JSON 文件持久化。若需多用户角色与权限控制，建议将导出后的静态站点部署至支持基础认证的 Web 服务器（如 Nginx 的 auth_basic 模块），或通过反向代理层实现简单的访问控制。未来路线图中考虑引入基于 SQLite 的多用户会话管理。

**问：如何迁移已录入的链接数据到另一台服务器？**

链接数据默认存储在 `data/links.json` 与 `data/categories.json` 文件中。您可以直接复制这两个文件至新环境的对应目录，重启服务后即完成迁移。若需更结构化的备份，可使用管理面板中的“导出全部数据”功能，生成包含时间戳的 JSON 归档包，再通过“导入”功能恢复。

**问：链接可用性检测是否会对外部站点造成访问压力？**

检测模块默认采用间隔至少 60 秒的轮询策略，且仅发送无请求体的 HEAD 请求，不下载页面内容。对于每个域名，每日检测次数不超过 1440 次（即每分钟一次），实际默认间隔为 300 秒（5 分钟），远低于普通搜索引擎爬虫的访问频率。若需调整检测间隔或超时时间，可修改 `config/validator.js` 中的参数。

## 许可证

本项目采用 MIT 许可证进行开源。您可以在遵守许可证条款的前提下，自由使用、修改、分发本软件，包括用于商业目的。详细的授权与限制说明请参阅项目根目录下的 `LICENSE` 文件。MIT 许可证全文亦可通过 <code>opensource.org/licenses/MIT</code> 获取。

> 外链数量: 7 | 生成时间: 2026-07-22 11:10:39
